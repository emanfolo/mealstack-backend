// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

//add below to env file
//DATABASE_URL="postgres://bazaetrrlwsdpd:4df328c360b414b2bff2c1c62d98a23d791f95c87f3315bda289cce96e16bcfd@ec2-34-242-89-204.eu-west-1.compute.amazonaws.com:5432/deor13s3g2470b"
//SHADOW_DATABASE_URL="postgres://nceqgnacsrgxsd:7b405df014f8e53358694065da448edb71433a5aed3b4ebf30527bd43155c565@ec2-54-195-76-73.eu-west-1.compute.amazonaws.com:5432/dah7300pr8dnft"
//below is a site that allows you to view the cloud db hosted on heroku
//https://heroku-data-explorer.herokuapp.com/

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

model Plan {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  calories  Int
  protein   Int
  carbs     Int
  fat       Int
  recipes   PlansOnRecipes[]
}

model Recipe {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  name      String   @db.VarChar(255)
  breakfast Boolean
  lunch     Boolean
  dinner    Boolean
  calories  Int
  protein   Int
  carbs     Int
  fat       Int
  ingredients String
  image_url String
  instructions_url String
  Type      String
  cuisine   String
  plans     PlansOnRecipes[]
}

model PlansOnRecipes {
  recipe       Recipe     @relation(fields: [recipeId], references: [id])
  recipeId     Int // relation scalar field (used in the `@relation` attribute above)
  plan         Plan @relation(fields: [planId], references: [id])
  planId       Int // relation scalar field (used in the `@relation` attribute above)
  assignedAt DateTime @default(now())
  assignedBy String

  @@id([recipeId, planId])
}